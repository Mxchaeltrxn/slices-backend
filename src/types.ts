export enum Role {
  NextDrawerAndNextRevealer,
  CurrentDrawer,
  CurrentRevealer,
  NextDrawer,
  NextRevealer,
  None,
}

export enum Phase {
  ChoosingWord,
  Drawing,
  ChoosingSlice,
  RevealedSlice,
  Waiting,
  Finished,
}
